#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running tests before push..."

# Run tests for the workspace
# During early development (Phase 0/1), allow push even if no tests exist
npm run test 2>&1 || true

echo "✅ Proceeding with push."
exit 0
